.form
  %p
    Город
  .form-group
    = select(@package, "pup_code", @cities.map { |city| [ [city["UniqName"], city["Region"], "Область"].join(" "), city["Code"] ] }, { include_blank: true}, id: "city_code", class: 'form-control',)
  .form-group
    .response
      Result:
      #result

:javascript
  $( '#city_code' ).change(function() {
    var input = $( "#city_code option:selected" ).val();
    console.log = input;
    $('#result').text(input);
    // $.ajax({type: "GET",
    //         url: "/packages/pds",
    //         data: input,
    //         dataType: 'script'});
  });
