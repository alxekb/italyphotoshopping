= render 'shared/profile/navbar'
.container
  %h1 Лоты

  %table.table
    %thead
      %tr
        %th Item
        %th Brand
        %th Color
        %th Price
        %th Cost
        %th
        %th
        %th

    %tbody
      - @profile_items.each do |profile_item|
        %tr
          %td= link_to profile_item.item, profile_item_path(profile_item)
          %td= link_to profile_item.brand, profile_item_path(profile_item)
          %td= profile_item.color
          %td= profile_item.price
          %td= profile_item.cost
          %td= link_to 'Show', profile_item
          -# %td= link_to 'Edit', edit_profile_item_path(profile_item)
          -# %td= link_to 'Destroy', profile_item, method: :delete, data: { confirm: 'Are you sure?' }

  %br

  = link_to 'New Item', new_profile_item_path
