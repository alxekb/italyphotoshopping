.container
  %h1
    All items
  .block
    - @items.each do |item|
      = image_tag("furla_julia_bag.jpeg", size: "90x120")
      = item.item
      = item.brand
      = item.name
      = link_to 'Show', item_path(item)
      %hr
